// Configuration for the frontend application
const config = {
  // Default hostname for instance URLs
  // This can be overridden by instance-specific hostname settings
  defaultHostname: 'inference.vm',
  
  // API base URL (relative, will use proxy in development)
  apiBaseUrl: '/api'
};

export default config;